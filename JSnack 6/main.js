
//creare un ciclo for fino a 1000 per poi andare a fare la potenza su tutti i suoi numeri.
for (var i = 0; i < 1001; i++) {
     //variabile contenente i numeri
     var numero = i ;
     //variabile per la potenza dei numeri
     var potenza_di_numero = numero * numero;
     console.log("Calcola la potenza del numero : " + numero + " = " + potenza_di_numero)
}
